import {utils} from 'metronic-extension';

// (async () => {
//   let dataUrl = await utils.fetchDataUrl('img/avatar2.png');
//   console.log('dataUrl=', dataUrl.slice(0,30));

//   dataUrl = await utils.fetchDataUrl('img/avatar1.svg');
//   console.log('dataUrl=', dataUrl.slice(0,30));

//   let img = await utils.fetchImage('img/avatar2.png');// <img crossorigin="use-credentials" src="img/avatar2.png">
//   console.log(img);
// })();