// NOTE: If the actual bootstrap is loaded, both the loaded bootstrap and the metronic bootstrap of the page using the metronic theme will be executed in duplicate, causing problems, so this package uses a dummy bootstrap.
export default window.bootstrap;