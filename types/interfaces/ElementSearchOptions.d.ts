/**
 * Element collection search options.
 */
export default interface ElementSearchOptions {
    /**
     * If true, the element collection is obtained as an HTML element; if false, the element collection is obtained as a JQuery object.
     * Default is false.
     */
    asHtmlElement?: boolean;
}
